# n8n Webhook URL that the Streamlit app sends user messages to
N8N_WEBHOOK_URL=https://your-n8n-instance/webhook/assistant

# Optional: request timeout (in seconds) for calls to the webhook
REQUEST_TIMEOUT=30
